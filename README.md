IndexTREC
==============

A simple utility to index TREC collections using Lucene.


Usage:

install java (JDK) if needed
install maven if needed
mvn compile
mvn exec:java -Dexec.args="<location of TREC files, e.g., gov2> <output directory for the index>"


